---
phase: 37-victory-scene-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/systems/MusicManager.js
autonomous: true
requirements: [VICT-11]

must_haves:
  truths:
    - "Victory music is the most emotional moment in the game -- epic, uplifting, with a memorable melody"
    - "Music has distinct sections that build from quiet contemplation to triumphant climax"
    - "Music loops seamlessly for the duration of the victory scene pages"
  artifacts:
    - path: "superzion/src/systems/MusicManager.js"
      provides: "Rewritten playVictoryMusic() with epic emotional score"
      contains: "playVictoryMusic"
  key_links:
    - from: "superzion/src/scenes/VictoryScene.js"
      to: "MusicManager.playVictoryMusic()"
      via: "MusicManager.get().playVictoryMusic() call in create()"
      pattern: "playVictoryMusic"
---

<objective>
Compose the most emotional music in the game for the victory scene -- an epic, uplifting, looping score with a memorable melody that builds from quiet reflection to triumphant celebration.

Purpose: The victory scene is the emotional payoff of the entire game. The current victory music is a simple 8-second D-major pad with a short ascending arpeggio -- it needs to become a full epic composition that moves the player.
Output: Rewritten playVictoryMusic() and _generateVictoryMusic() in MusicManager.js
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@superzion/src/systems/MusicManager.js
</context>

<interfaces>
<!-- Key patterns from MusicManager.js the executor needs -->

Looping music pattern:
```javascript
_startLoop(trackName, loopDuration, generator, vol = 0.5)
// generator(startTime) is called every loopDuration seconds
// All oscillators/gains must be pushed to this._nodes for cleanup
```

Available synth primitives:
```javascript
_kick(t, vol)        // Trance kick drum
_hihat(t, vol)       // Closed hi-hat
_openHat(t, vol)     // Open hi-hat
_snare(t, vol)       // Snare
_acidBass(t, freq, dur, vol)  // Sawtooth through resonant lowpass
_pad(t, freq, dur, vol)       // Detuned sawtooth pad (5 voices)
_lead(t, freq, dur, vol)      // Square wave with filter sweep
_arp(t, freq, dur, vol, type) // Short note (triangle/sine/square)
_clap(t, vol)        // Layered noise bursts
```

Note constants available:
```javascript
const NOTE = { A2:110, ..., A3:220, ..., C4:261.63, D4:293.66, E4:329.63, ..., A5:880 }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Compose epic victory music with looping emotional score</name>
  <files>superzion/src/systems/MusicManager.js</files>
  <action>
Rewrite playVictoryMusic() and _generateVictoryMusic() to create a looping epic score.

**Use _startLoop pattern** (like playMenuMusic, playLevel3Music) instead of the current one-shot _generateVictoryMusic approach. This ensures the music loops seamlessly for however long the player reads through the victory pages.

**Key: D major (D, F#/Gb, A) -- the brightest key in the game.**
**BPM: ~90 (majestic, not frantic)**
**Loop length: 8 bars (~21 seconds)**

**Musical structure within the 8-bar loop:**

Bars 1-2: Quiet contemplation
- Gentle D major pad (D4, Gb4, A4) at low volume
- Soft sub bass (D3 sine) with slow swell
- Single slow arpeggio: D4 -> Gb4 -> A4 -> D5 (triangle, quiet)
- No drums -- just pads and gentle notes

Bars 3-4: Building hope
- Pad volume increases slightly
- Add a second pad voice (A3, D4, Gb4) for richness
- Memorable 8-note melody on lead synth (square wave through lowpass):
  D5, E5, Gb5, A5, Gb5, E5, D5, A4
  (ascending to the fifth then descending -- hopeful arc)
- Soft kick enters every 2 beats
- Light hi-hats at eighth notes

Bars 5-6: Triumphant rise
- Full kick on every beat
- Acid bass enters: D3 -> A3 -> Gb3 -> D3 progression
- Lead melody repeats but an octave segment higher on the peak notes
- Add claps on beats 2 and 4
- Open hats on offbeats
- Ascending arp run: D4 Gb4 A4 D5 Gb5 A5 (sixteenth notes, fast)

Bars 7-8: Climax and resolve
- Everything at full volume
- Lead plays the melody variation: A5, Gb5, D5, A5, D5*2, A5, Gb5, D5
  (peaks at D6 = most emotional moment)
- Snare fills on bar 8 (building tension for loop restart)
- Extra pad shimmer: high A5 pad whisper
- Bar 8 last beat: brief silence/drop to create breathing room before loop restarts

**Replace the existing one-shot approach:**
- Delete the current _generateVictoryMusic() method
- Rewrite playVictoryMusic() to use _startLoop('victory', loopLen, generator, 0.55)
- The loop seamlessly restarts, matching how all other tracks work

**Volume:** 0.55 (slightly louder than menu music at 0.4, matching cinematic importance)

Do NOT change any other music methods. Only modify playVictoryMusic() and _generateVictoryMusic().
  </action>
  <verify>
    <automated>cd C:/Users/Seba/Desktop/ORL3/superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - playVictoryMusic() uses _startLoop with ~21s loop at 90 BPM
    - Music builds from quiet pads (bars 1-2) through melody introduction (3-4) to triumphant climax (7-8)
    - Memorable lead melody present (D5-E5-Gb5-A5-Gb5-E5-D5-A4 motif)
    - Uses D major key (brightest in the game, contrasting dark villain D minor)
    - Build completes without errors
  </done>
</task>

</tasks>

<verification>
- Build succeeds: `cd superzion && npx vite build`
- playVictoryMusic exists and uses _startLoop pattern
- No other music methods were modified
</verification>

<success_criteria>
Victory music is a looping epic score in D major at ~90 BPM that builds from quiet contemplation to triumphant climax with a memorable lead melody, using the standard _startLoop pattern for seamless looping.
</success_criteria>

<output>
After completion, create `.planning/phases/37-victory-scene-redesign/37-01-SUMMARY.md`
</output>
