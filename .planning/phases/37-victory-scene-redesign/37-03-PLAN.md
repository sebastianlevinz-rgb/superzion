---
phase: 37-victory-scene-redesign
plan: 03
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - superzion/src/scenes/VictoryScene.js
autonomous: true
requirements: [VICT-05, VICT-06, VICT-07, VICT-08]

must_haves:
  truths:
    - "Celebrating people surround SuperZion: soldiers, civilians, children, dogs, cats"
    - "People hold Israel flags, are hugging, jumping, and clapping"
    - "Gold, blue, and white confetti falls continuously"
    - "Fireworks explode in the sky and fade with trails"
  artifacts:
    - path: "superzion/src/scenes/VictoryScene.js"
      provides: "Celebrating crowd, confetti system, firework system"
      contains: "_drawCelebrators"
  key_links:
    - from: "superzion/src/scenes/VictoryScene.js _drawCelebrators"
      to: "pages 7 and 8 setup functions"
      via: "this._drawCelebrators() call in page setup"
      pattern: "_drawCelebrators"
    - from: "superzion/src/scenes/VictoryScene.js _spawnConfetti"
      to: "pages 7 and 8 setup functions"
      via: "this._spawnConfetti() call in page setup"
      pattern: "_spawnConfetti"
---

<objective>
Fill in the celebrating crowd, confetti, and fireworks systems in VictoryScene.js -- replacing the stub methods left by Plan 02 with detailed implementations showing diverse celebrating people, Israel flags, continuous confetti, and animated fireworks.

Purpose: The celebration visuals are the emotional payoff -- seeing the people SuperZion fought for, celebrating victory together.
Output: Completed _drawCelebrators(), _spawnConfetti(), _stopConfetti(), _launchFirework() methods in VictoryScene.js
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-victory-scene-redesign/37-02-SUMMARY.md
@superzion/src/scenes/VictoryScene.js
</context>

<interfaces>
<!-- From BaseCinematicScene, available in VictoryScene -->

```javascript
// W = 960, H = 540
_addPageVisual(obj)  // Track for auto-cleanup
this.skipped         // Check if user skipped scene
this.tweens.add({...})  // Phaser tween system
this.time.addEvent({...})  // Phaser timer
this.time.delayedCall(ms, fn)  // Delayed callback
this.add.graphics()  // Phaser graphics object
this.add.rectangle(x, y, w, h, color, alpha)  // Rectangle
this.add.circle(x, y, radius, color, alpha)  // Circle
this.add.ellipse(x, y, w, h, color, alpha)  // Ellipse
this.cameras.main.shake(duration, intensity)  // Screen shake
Phaser.Math.Between(min, max)  // Random int
Phaser.Utils.Array.GetRandom(arr)  // Random array element

// SoundManager available:
import SoundManager from '../systems/SoundManager.js';
SoundManager.get().playExplosion()  // For firework burst sound
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Detailed celebrating crowd with diverse characters</name>
  <files>superzion/src/scenes/VictoryScene.js</files>
  <action>
Replace the stub `_drawCelebrators()` method with a detailed implementation. The crowd appears at the bottom of the screen, surrounding the hero position.

**Crowd composition (22-26 figures total):**

The crowd spans from x=40 to x=W-40, positioned at varying Y near the bottom (H-10 to H-30). Each figure is drawn with a graphics object at depth 15.

**Figure types (drawn procedurally, each 12-20px tall):**

1. SOLDIERS (4-5 figures):
   - Olive drab (#556b2f) body rectangle
   - Darker green helmet (small ellipse on head)
   - Slightly taller than civilians (18-20px)
   - Arms raised holding rifle (small vertical line above head)

2. CIVILIANS (6-8 figures):
   - Varied shirt colors: blue (#3355aa), red (#aa3333), white (#cccccc), gray (#888888)
   - Pants in dark colors (#333333, #222244)
   - Head: skin-toned circle (radius 3-4), use varied skin tones (#d2a679, #c49668, #b08657)
   - Some with arms up (celebrating), some at sides

3. WOMEN (3-4 figures):
   - Slightly smaller (14-16px)
   - Dress/skirt shape (wider triangular lower body instead of two legs)
   - Varied colors: blue, white, purple (#6644aa)
   - Hair: slightly larger head circle or small extension

4. CHILDREN (3-4 figures):
   - Much smaller (8-12px)
   - Bright shirt colors: yellow (#ffdd44), red, blue
   - Positioned between adults
   - All jumping (all get jump tweens)

5. DOGS (2 figures):
   - Small horizontal oval body (8x4px, brown #8B4513)
   - Small circle head (radius 2)
   - Tiny legs (1px lines)
   - Short upward line for tail
   - Positioned at ground level

6. CATS (2 figures):
   - Even smaller oval body (6x3px, gray #666666 or orange #cc7722)
   - Tiny triangle ears on head
   - Thin curved tail (arc line)
   - Positioned at ground level

**Israel flags (5-6 flags):**
Every 4th person holds a small Israel flag above their head:
- Pole: 1px wide, 12px tall, gray (#888888)
- White rectangle: 10x7px
- Two blue (#0038b8) horizontal stripes (top and bottom, 1.5px each)
- Tiny blue Star of David outline in center (optional -- very small, 2 lines suffice)
- Flag sways: angle tween from -8 to 8 degrees, sinusoidal, 600-900ms

**Animations:**
- ~40% of people have jump tweens (y moves up 4-8px, yoyo, 300-550ms, sine ease, random delay 0-500ms)
- ~50% of people (arms-up ones) have arm wave / rotation tween (angle -5 to 5, 250-400ms, yoyo, sine)
- Two pairs of people "hugging" -- draw them overlapping slightly with arms reaching toward each other
- Children always jumping (100% get jump tweens)

**Depth:** People at depth 15, flags at depth 16 (same as current scene convention).
  </action>
  <verify>
    <automated>cd C:/Users/Seba/Desktop/ORL3/superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _drawCelebrators() draws 22-26 figures: soldiers, civilians, women, children, dogs, cats
    - 5-6 Israel flags held by crowd members, swaying with animation
    - Jump, wave, and hug animations active on crowd figures
    - Dogs and cats visible at ground level
    - Build completes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Confetti system and enhanced fireworks</name>
  <files>superzion/src/scenes/VictoryScene.js</files>
  <action>
Replace the stub _spawnConfetti(), _stopConfetti(), and _launchFirework() methods.

**CONFETTI (_spawnConfetti / _stopConfetti):**

Gold, blue, and white particles falling continuously. Weighted toward gold (Israel colors).

Implementation:
- Use this.time.addEvent with delay 70ms, repeat -1
- Track timer in this._confettiTimer
- Track particle count; cap at ~50 on screen
- Each confetti piece: this.add.rectangle at random x (20 to W-20), y=-5
- Size: 2x4px or 3x5px (small rectangles)
- Colors array: [0xFFD700, 0xFFD700, 0xFFD700, 0x0055ff, 0x0055ff, 0xffffff, 0xffffff] (gold-weighted)
- Animations per piece:
  - Fall tween: y to H+10, duration 2500-4000ms, linear, onComplete destroy + decrement count
  - Horizontal drift: sine wave, x +/- 15-40px, 400-800ms period, yoyo repeat -1
  - Rotation: angle to random(-180,180), same duration as fall
  - Alpha fades to 0.3 during fall
- _addPageVisual each piece for cleanup

_stopConfetti: remove timer, set null. Same pattern as current implementation.

**FIREWORKS (_launchFirework):**

Enhanced fireworks with rocket trail, burst, and fade. Same high-quality pattern as the current implementation but keep it (it was already good from Phase 27).

Implementation:
- Rocket phase: white circle (radius 3) at random x (80 to W-80), starting at H+10
  - Trail timer: every 40ms, spawn small fading dot behind rocket
  - Tween rocket upward to random peak y (60-200), duration 600ms, cubic ease out
- Burst phase (on rocket arrival):
  - Destroy rocket, stop trail timer
  - Pick 2 random colors from [0xFFD700, 0x0055ff, 0xffffff, 0xff3333, 0x33ff55, 0xff66cc]
  - Spawn 24 particles in radial pattern
  - Each particle: circle radius 2.5, tweens outward (spread 50 + random 0-30px) with alpha fade to 0, duration 700-900ms
  - Trail effect: 60% chance each frame to spawn tiny fading dot at particle position
  - Central flash: white circle radius 8, scales up 3x while fading, duration 300ms

For pages 7 and 8, the page setup should already be calling these methods. Verify the stubs from Plan 02 are being called. If page 7 setup does not call _spawnConfetti and _launchFirework, add those calls. Page 8 should have more fireworks (stagger 10 launches over 400ms intervals).

Add _drawFlag(cx, cy) method as well since it is used on page 8 for flanking flags:
- Pole: 3px wide, fh+30 tall, gray
- White flag: 80x55px
- Two blue stripes
- Blue Star of David outline in center
- Fade-in tween + gentle sway tween (scaleX 0.96, sinusoidal)

Ensure page 7 setup calls: _drawSunrise(1.0), _drawClouds(1.0), _drawForwardHero(), _drawGiantStar(), _drawCelebrators(), _spawnConfetti(), and launches 4 fireworks.
Ensure page 8 setup calls: _drawSunrise(1.0), _drawClouds(1.0), _drawCelebrators(), _spawnConfetti(), _drawFlag on both sides, title text, subtitle text, and launches 10 staggered fireworks.
Ensure page 8 cleanup (and page 7 cleanup) calls _stopConfetti().

Also add SoundManager.get().playExplosion() on page 7 (Am Yisrael Chai moment) with camera shake, and on page 8 with camera shake.
  </action>
  <verify>
    <automated>cd C:/Users/Seba/Desktop/ORL3/superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _spawnConfetti creates continuous gold/blue/white confetti particles (capped at ~50)
    - _stopConfetti properly cleans up timer
    - _launchFirework creates rocket trail + radial burst + central flash
    - _drawFlag creates Israel flag with sway animation
    - Page 7 (Am Yisrael Chai) has confetti + 4 fireworks + crowd + screen shake
    - Page 8 (final) has confetti + 10 staggered fireworks + flanking flags + title + subtitle
    - Camera shake on pages 7 and 8
    - Build completes without errors
  </done>
</task>

</tasks>

<verification>
- Build succeeds: `cd superzion && npx vite build`
- _drawCelebrators contains figure types: soldier, civilian, children, dog, cat
- _spawnConfetti creates gold/blue/white particles
- _launchFirework creates rocket+burst pattern
- Page 7 and 8 setup functions call all visual methods
</verification>

<success_criteria>
Victory scene pages 7-8 show celebrating diverse crowd (soldiers, civilians, children, dogs, cats) with Israel flags, continuous gold/blue/white confetti falling, animated fireworks exploding and fading in the sky, camera shake on emotional peaks, and Israel flags flanking the final title screen.
</success_criteria>

<output>
After completion, create `.planning/phases/37-victory-scene-redesign/37-03-SUMMARY.md`
</output>
