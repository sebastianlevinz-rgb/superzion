---
phase: 34-level-4-complete-redesign
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - superzion/src/scenes/DroneScene.js
  - superzion/src/utils/DroneTextures.js
autonomous: true
requirements: [LVL4-07, LVL4-08, LVL4-09, LVL4-10, LVL4-11]

must_haves:
  truths:
    - "Boss fight room is a destroyed building interior with daytime light visible through holes in walls and ceiling"
    - "Room contains detailed debris: broken lamps, cracked mirrors, overturned tables, broken chairs, rubble piles, hanging cables, dust in air"
    - "The Warden sits in an individual armchair (single seat, NOT a sofa or couch)"
    - "The Warden hides behind the armchair, peeks out to throw objects at the drone, then hides again in a repeating cycle"
    - "SHIFT dodge/dash works during the boss fight — pressing SHIFT makes the drone dash laterally with brief invulnerability"
  artifacts:
    - path: "superzion/src/scenes/DroneScene.js"
      provides: "Boss room rendering with daytime light, armchair cover mechanic, peek-throw-hide AI"
      contains: "armchair"
    - path: "superzion/src/utils/DroneTextures.js"
      provides: "Armchair texture and daytime command room texture"
      contains: "createArmchairTexture"
  key_links:
    - from: "Boss AI state machine"
      to: "Armchair cover position"
      via: "boss state: behind_cover -> peeking -> throwing -> behind_cover"
      pattern: "behind_cover|peeking"
    - from: "SHIFT key handler"
      to: "Dodge mechanic"
      via: "dodgeActive flag and invulnerability window"
      pattern: "dodgeActive"
---

<objective>
Rework the boss fight phase: replace the couch/multi-cover system with a single individual armchair where The Warden hides, peeks to throw objects, and hides again. Ensure the room visuals show daytime light through holes and detailed debris. Verify SHIFT dodge/dash still works.

Purpose: Requirements LVL4-07 through LVL4-11 demand specific boss room aesthetics (daytime interior) and a specific boss behavior pattern (armchair hide-peek-throw cycle) plus working dodge mechanic.
Output: Boss fight in DroneScene.js reworked with armchair cover and daytime room visuals.
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-level-4-complete-redesign/34-01-SUMMARY.md
@superzion/src/scenes/DroneScene.js
@superzion/src/utils/DroneTextures.js
</context>

<interfaces>
<!-- From Plan 01, the following new textures will be available -->
From superzion/src/utils/DroneTextures.js (created by Plan 01):
```javascript
export function createCommandRoom(scene)          // 960x540, daytime room with light through holes
export function createArmchairTexture(scene)       // 80x80, individual armchair front view
export function createArmchairSideTexture(scene)   // 60x80, armchair side view for peek animation
```

From superzion/src/scenes/DroneScene.js (existing boss fight structure):
```javascript
// Boss constants (front-facing perspective) — lines ~39-55
const BOSS_DISPLAY = 100;      // boss display size inside room
const BOSS_BASE_HP = 30;
const BOSS_SPEED_NORMAL = 60;
const BOSS_THROW_INTERVAL_MIN = 1.5;
const BOSS_THROW_INTERVAL_MAX = 2.5;
const BOSS_THROW_SPEED = 250;
const BOSS_THROW_DMG = 2;

// Cover positions — line 63 (currently 3 positions: couch, rubble, column)
const COVER_POSITIONS = [
  { x: W / 2, y: COUCH_Y, type: 'couch' },
  { x: 250, y: 280, type: 'rubble' },
  { x: 710, y: 250, type: 'column' },
];

// Boss state machine cases — line 3044+
case 'exposed':
case 'moving_to_cover':
case 'behind_cover':
case 'peeking':

// Dodge mechanic — lines 3666-3700
this.dodgeActive, this.dodgeCooldown, this.dodgeTimer
SHIFT key handler with directional dash and invulnerability window
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rework boss room visuals and armchair cover system</name>
  <files>superzion/src/scenes/DroneScene.js, superzion/src/utils/DroneTextures.js</files>
  <action>
**Part A: Verify/enhance daytime command room texture in DroneTextures.js**

The createCommandRoom() function was rewritten in Plan 01 for daytime. If it does not yet include all required LVL4-08 debris details, add them now:
- Broken lamps: at least 2 bent floor lamp shapes (one near left wall, one tilted on floor)
- Cracked mirror: reflective shard area on back wall (light gray irregular polygon with bright highlights)
- Overturned tables: 2 rectangular table shapes tilted at angles on the floor
- Broken chairs: 2-3 chair shapes scattered (legs up, seats sideways)
- Additional rubble piles, cables hanging from ceiling, dust motes
- Daytime light: bright patches on floor from holes in ceiling (2-3 light rectangles angled)
- Holes in walls showing bright blue sky outside

If these elements are already present from Plan 01's implementation, verify they exist and enhance if thin.

**Part B: Rework boss cover system in DroneScene.js**

1. **Replace COVER_POSITIONS constant** — change from 3 positions to a SINGLE armchair position:
```javascript
// Single armchair cover — center of room
const ARMCHAIR_X = W / 2;
const ARMCHAIR_Y = 320;  // slightly forward in room
```
Remove the old COVER_POSITIONS array, COUCH_Y constant, and any multi-cover logic.

2. **Remove inline couch/rubble/column texture generators** — delete `_generateCouchTexture()`, `_generateRubbleTexture()`, `_generateColumnTexture()` and their calls in create(). Replace with the imported `createArmchairTexture()` and `createArmchairSideTexture()` already called in create() by Plan 01.

3. **Rework _startBossFight():**
   - Place a single armchair sprite/graphic at ARMCHAIR_X, ARMCHAIR_Y using the 'armchair' texture
   - The Warden starts behind the armchair (hidden state)
   - Boss initial state: 'behind_cover'
   - Set up the armchair as the only cover object
   - Keep boss HP, drone HP, weapon systems from current implementation

4. **Rework boss state machine (_updateBoss):**
   Simplify to a clear 3-state cycle around the SINGLE armchair:

   **State: 'behind_cover'** (hidden behind armchair)
   - Boss sprite NOT visible (or rendered small/behind armchair)
   - Timer counts down (1.5-3.0 seconds random)
   - When timer expires: transition to 'peeking'
   - Draw only the armchair (boss behind it)

   **State: 'peeking'** (boss peeks over/around armchair)
   - Boss face appears above or to the side of the armchair
   - Use armchair_side texture to show the chair from the side view
   - Short peek duration (0.5s) before throwing
   - Player can shoot the boss during peek (vulnerable window)
   - After peek delay: throw an object at the drone
   - Boss face shows 'angry' or 'furious' expression based on HP

   **State: 'throwing'** (new explicit state — boss throws then retreats)
   - Boss throws a projectile (rock, pipe, debris) toward the drone
   - Throw animation: boss arm extends, projectile launches
   - Immediately after throw: transition back to 'behind_cover'
   - Boss becomes invulnerable again once behind armchair

   Remove: 'exposed', 'moving_to_cover' states (boss no longer moves between multiple cover positions). The boss stays at the ONE armchair location throughout the fight.

   **Phase escalation by HP:**
   - Above 50% HP: normal pace (2-3s hide, peek, throw, repeat)
   - Below 50% HP ('angry'): faster pace (1-1.5s hide), throws 2 objects per peek, boss face turns angry
   - Below 20% HP ('furious'): very fast (0.5-1s hide), throws 3 objects, adds melee charge attacks where boss rushes from behind armchair toward drone then runs back
   - On death: boss collapses in front of armchair, 'dead' face expression

5. **Rework _drawBoss():**
   - Always draw the armchair at ARMCHAIR_X, ARMCHAIR_Y
   - When 'behind_cover': draw only armchair, maybe show boss hat/keffiyeh peeking slightly above
   - When 'peeking': draw boss head/torso visible above/beside armchair, use boss expression texture
   - When 'throwing': draw boss in throw pose with extended arm
   - Add room debris overlay elements drawn on top of the command_room background:
     - Dust particles floating (small circles with low alpha, gently drifting)
     - Occasional rubble falling from ceiling (small rectangles dropping slowly)

6. **Preserve all existing mechanics that work:**
   - Player Z key shoots bullets into the room
   - X key fires missiles for heavy damage
   - Boss throws objects that fly TOWARD the drone (growing in size as they approach camera)
   - HUD shows boss HP bar, drone HP, cooldowns
   - Victory condition: boss HP reaches 0
   - Defeat condition: drone HP reaches 0
   - SHIFT dodge/dash — do NOT modify the dodge code (lines ~3666-3700), it already works

**Part C: Verify SHIFT dodge works**
The existing dodge mechanic (SHIFT key triggers a 250ms lateral dash with invulnerability) must continue to function. Do NOT change the dodge code. Simply ensure:
- The keys.shift input is still registered in _setupInput()
- The dodge update logic in the boss phase update is still called
- The dodge HUD (SHIFT label, cooldown bar) is still drawn
- The dodge visual feedback (brief tint or alpha change) still works

**IMPORTANT CONSTRAINTS:**
- Preserve scene key 'DroneScene'
- Preserve nextScene: 'MountainBreakerIntroCinematicScene'
- Preserve EndScreen integration (showVictoryScreen, showDefeatScreen)
- Preserve MusicManager calls
- Preserve difficulty scaling (DifficultyManager)
- Do NOT touch the city phase code created by Plan 01
  </action>
  <verify>
    <automated>cd C:/Users/Seba/Desktop/ORL3 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Boss fight room shows daytime interior with light through holes. Debris details visible (broken lamps, mirrors, tables, chairs, rubble, cables). Single armchair (not sofa) as The Warden's cover. Boss follows hide-peek-throw cycle behind the armchair. SHIFT dodge/dash functional. Build succeeds. Victory and defeat screens work.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end Level 4 smoke test via build verification</name>
  <files>superzion/src/scenes/DroneScene.js</files>
  <action>
Run the production build and verify no errors or warnings. Then do a quick code audit:

1. **Build verification:** Run `npx vite build` and confirm zero errors.

2. **Import audit:** Verify DroneScene.js imports match DroneTextures.js exports — no missing or unused imports.

3. **Phase flow audit:** Confirm the phase transition chain is:
   - create() -> _startCityIntro() sets phase='city'
   - City phase completion -> transition -> _startBossFight() sets phase='boss'
   - Boss defeat -> _showVictory() sets phase='victory'
   - No references to 'recon' or 'tunnel' phases remain

4. **Texture key audit:** Confirm all texture keys referenced in DroneScene.js drawing code exist in DroneTextures.js:
   - 'drone_top' (createDroneSprite)
   - 'daytime_sky' (createDaytimeSky)
   - 'command_room' (createCommandRoom)
   - 'armchair' (createArmchairTexture)
   - 'armchair_side' (createArmchairSideTexture)
   - Boss face textures: 'ts_boss4_normal', 'ts_boss4_angry', 'ts_boss4_furious', 'ts_boss4_dead' (inline generators)

5. **Controls audit:** Verify controls text shows current controls for each phase:
   - City phase: arrows + shoot + enter
   - Boss phase: arrows + shoot + missile + dodge

6. **Fix any issues found.** If code references removed textures (tunnel_wall, tunnel_floor, etc.), remove those references. If boss code still references 'couch' texture key or COVER_POSITIONS array, fix to use armchair.

7. **Console.log cleanup:** Remove any debug console.log statements that might have been added during development. Keep only essential error handling.
  </action>
  <verify>
    <automated>cd C:/Users/Seba/Desktop/ORL3 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Production build succeeds with zero errors. No references to tunnel/recon/couch remain. All texture keys match between scene and texture files. Phase flow is city->boss->victory. Controls overlay correct for each phase. No debug console.log statements.</done>
</task>

</tasks>

<verification>
1. `npx vite build` succeeds without errors
2. Boss room shows daytime interior lighting (not dark/night)
3. Visible holes in walls/ceiling showing blue sky outside
4. Room debris: broken lamps, cracked mirrors, overturned furniture, rubble, cables
5. Single armchair (not sofa) as The Warden's cover
6. Boss cycle: hide behind armchair -> peek out -> throw objects -> hide again
7. SHIFT key triggers dodge dash with brief invulnerability
8. Boss fight reaches victory and defeat conditions correctly
9. No references to tunnel/recon phases in code
</verification>

<success_criteria>
- Boss room visuals show destroyed interior with daytime light through holes in walls/ceiling
- Detailed debris items present: lamps, mirrors, tables, chairs, rubble, cables, dust
- The Warden uses an individual armchair (mono-sillon) as cover, NOT a sofa or couch
- Boss AI follows clear hide-peek-throw-hide pattern behind the armchair
- SHIFT dodge/dash works: lateral dash with brief invulnerability, cooldown bar visible
- Full Level 4 flow works: daytime city scroll RIGHT -> window entry -> boss fight -> victory
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-level-4-complete-redesign/34-02-SUMMARY.md`
</output>
