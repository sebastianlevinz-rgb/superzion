---
phase: 36-supreme-turban-cinematic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/systems/MusicManager.js
  - superzion/src/utils/ParadeTextures.js
  - superzion/src/scenes/LastStandCinematicScene.js
autonomous: true
requirements: [CINE-01, CINE-02, CINE-03]

must_haves:
  truths:
    - "Dark dramatic music plays during Supreme Turban reveal -- slow grave drums, ominous low brass, minor key"
    - "Background shows a detailed war scene with fire, smoke, army silhouettes, missile trucks, soldiers, stacked armaments"
    - "Supreme Turban appears larger and more imposing -- brighter red eyes, glowing crescent moon staff, more sprite detail"
  artifacts:
    - path: "superzion/src/systems/MusicManager.js"
      provides: "playVillainMusic() method"
      contains: "playVillainMusic"
    - path: "superzion/src/utils/ParadeTextures.js"
      provides: "Enhanced Supreme Turban sprite with brighter eyes and glowing staff"
      contains: "parade_supremeturban"
    - path: "superzion/src/scenes/LastStandCinematicScene.js"
      provides: "War background visuals and imposing villain reveal"
      contains: "playVillainMusic"
  key_links:
    - from: "superzion/src/scenes/LastStandCinematicScene.js"
      to: "MusicManager.playVillainMusic()"
      via: "MusicManager.get().playVillainMusic()"
      pattern: "playVillainMusic"
    - from: "superzion/src/scenes/LastStandCinematicScene.js"
      to: "parade_supremeturban texture"
      via: "this.add.sprite with setScale(2.5+)"
      pattern: "parade_supremeturban.*setScale"
---

<objective>
Make the Supreme Turban cinematic scene terrifying and imposing: dark dramatic music with slow grave drums and ominous brass, a detailed war background with fire/smoke/army/missiles, and a larger more detailed villain sprite with brighter glowing red eyes and glowing crescent staff.

Purpose: This is the final villain reveal before the last level. It must feel like the stakes are at their highest -- dark, dramatic, dangerous.
Output: Modified MusicManager.js (new playVillainMusic method), enhanced ParadeTextures.js (brighter Supreme Turban sprite), rewritten LastStandCinematicScene.js (war background + imposing reveal).
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files to read before implementation:
@superzion/src/systems/MusicManager.js (music engine -- add playVillainMusic after playVictoryMusic at line ~1108)
@superzion/src/utils/ParadeTextures.js (line 610-800 -- createSupremeTurbanParade function to enhance)
@superzion/src/scenes/LastStandCinematicScene.js (99 lines -- complete scene to rewrite)
@superzion/src/scenes/BaseCinematicScene.js (page system base class -- _initPages, _addPageVisual, _handlePageInput)

<interfaces>
<!-- From BaseCinematicScene.js -->
export { W, H }; // W=960, H=540
export default class BaseCinematicScene extends Phaser.Scene {
  _initCinematic()
  _initPages(pages, targetScene, targetSceneData)
  _handlePageInput()
  _addPageVisual(obj)
  _clearPageVisuals()
}
// Page format: { text, color?, size?, y?, charDelay?, setup?, cleanup? }

<!-- From MusicManager.js -->
export default class MusicManager {
  static get()
  _startLoop(trackName, loopDuration, generator, vol)
  _kick(t, vol)    // Sine pitch sweep 150->40Hz
  _snare(t, vol)   // Noise + triangle body
  _hihat(t, vol)   // Filtered noise
  _pad(t, freq, dur, vol)  // 5-voice detuned sawtooth through lowpass
  _arp(t, freq, dur, vol, type)
  _acidBass(t, freq, dur, vol)
  _lead(t, freq, dur, vol)
  playCinematicMusic(levelNum)  // Currently used by LastStandCinematicScene
  playVictoryMusic()  // Last music method before class end
}
// NOTE frequencies available: A2(110) through A5(880)
// SCALES: minorHarmonic, minor, phrygian

<!-- From ParadeTextures.js -->
export function createSupremeTurbanParade(scene)
// Creates 'parade_supremeturban' texture key
// Sprite dimensions: uses staticSprite helper, canvas 80x160
export function generateAllParadeTextures(scene)
// Generates all parade sprites including Supreme Turban
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark villain music and enhanced Supreme Turban sprite</name>
  <files>superzion/src/systems/MusicManager.js, superzion/src/utils/ParadeTextures.js</files>
  <action>
**MusicManager.js -- Add playVillainMusic() method** (insert after playVictoryMusic / _generateVictoryMusic, before the closing brace of the class at ~line 1137):

Create a looping music track that conveys final danger. Design:
- BPM: 55 (very slow, grave)
- Key: D minor (D3 root) using phrygian scale for maximum darkness
- Loop length: 4 bars of 4 beats each
- Structure:
  1. **Grave drums**: Use _kick() with high volume (0.4) on beats 1 and 3 of each bar with long decay feel. Add a second _kick one octave lower (modify frequency inline -- create an oscillator at 60Hz with slow decay for sub-boom). Every 2 bars, add a _snare hit (0.08 vol) on beat 4 for a military feel.
  2. **Ominous brass**: Use _pad() at D2 (73.4Hz), F2 (87.3Hz), and A2 (110Hz) -- D minor triad in a very low register. Set volume to 0.08 each for thick dark drone. The pad's sawtooth-through-lowpass already sounds brass-like at low frequencies.
  3. **Tension arpeggio**: Every 2 bars, play a slow 4-note descending arpeggio: D4, C4, Bb3, A3 using _arp with 'sawtooth' type, each note = 1 beat duration, volume 0.035. This descending minor pattern creates dread.
  4. **Sub bass drone**: Create a raw sine oscillator at D2 (73.4Hz) held for the full loop duration with volume 0.06, slow fade in/out.
  5. Volume: 0.5 (standard)

Method signature: `playVillainMusic()` -- no parameters needed.

Use `this._startLoop('villain', loopLen, (startT) => { ... }, 0.5)` pattern exactly like other music methods.

For the D2 and F2 frequencies not in the NOTE constant, calculate inline: `NOTE.D3 / 2` for D2 (73.4Hz), `NOTE.F3 / 2` for F2 (87.3Hz). Bb3 = `NOTE.A3 * Math.pow(2, 1/12)` (~233Hz).

**ParadeTextures.js -- Enhance Supreme Turban sprite** in createSupremeTurbanParade():

Make these targeted changes to the existing drawing code (do NOT rewrite from scratch -- modify specific sections):

1. **Brighter red eyes** (around line 714-720): Change eye fill from `#ff2200` to `#ff4400` and increase eye radius from 2 to 3. Increase eye glow radius from 6 to 10 and glow opacity from 0.3 to 0.5. Add a second outer glow ring at radius 14, opacity 0.2.

2. **Glowing crescent staff** (around line 648-668): Change crescent fill from `#8a7020` to `#ffaa00` (bright gold). Change staff glow from `rgba(255, 80, 0, 0.3)` radius 16 to `rgba(255, 160, 0, 0.5)` radius 22. Add a second glow layer at radius 30 with `rgba(255, 120, 0, 0.2)`. Add pulsing light dots: 3 small circles (radius 2) along the staff at intervals, filled with `rgba(255, 200, 50, 0.4)`.

3. **More detail**: Add faint claw-like fingers on the hands (3 thin strokes per hand in dark color). Add subtle smoke/aura around the base of the robes: 4-5 semi-transparent dark circles (`rgba(30, 0, 0, 0.15)`) at random-ish positions below the robe hem.
  </action>
  <verify>
    <automated>cd C:/Users/Seba/Desktop/ORL3/superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>MusicManager.js has playVillainMusic() method that produces slow grave drums + ominous brass drone + tension arpeggio in D minor at 55 BPM. ParadeTextures.js Supreme Turban has brighter red eyes (larger glow), bright gold glowing crescent staff (double glow rings), claw fingers, and dark smoke aura at base.</done>
</task>

<task type="auto">
  <name>Task 2: War background and imposing villain reveal scene</name>
  <files>superzion/src/scenes/LastStandCinematicScene.js</files>
  <action>
Rewrite LastStandCinematicScene.js to replace the current simple backgrounds with a detailed war scene and imposing villain reveal. Keep the same page-based structure (6 pages, same flow: Natanz crater -> darkness -> Supreme Turban reveal -> fortress -> history -> hero).

Key changes:

1. **Replace `playCinematicMusic(6)` with `playVillainMusic()`** on line 17. This is the single most important change for CINE-01.

2. **Page 1** (Natanz crater) -- keep largely the same but add more fire/smoke detail to the explosion aftermath. Add flickering orange glow effect using a timed tween on a semi-transparent circle.

3. **Page 3** (Supreme Turban reveal -- currently lines 50-67) -- THIS IS THE KEY PAGE for CINE-02 and CINE-03. Replace the simple dark red background with a full detailed war scene background:

   Using Phaser graphics (this.add.graphics()):

   a. **Sky**: Dark red-orange gradient (blood sky) from top to ~40% height. Colors: dark crimson (#1a0505) at top fading to smoky orange (#3a1505) at horizon.

   b. **Ground**: Dark earth from 70% to bottom, color #1a0a05.

   c. **Fire and smoke**: 6-8 fire columns at varying X positions along the horizon line (y ~ H*0.55-0.65). Each fire = orange-to-yellow vertical gradient ellipse (fillStyle rgba(255,100,0,0.6), fillRect or fillCircle). Above each fire, a smoke plume = 2-3 stacked semi-transparent dark gray circles (rgba(40,30,20,0.4)) rising upward, each larger than the one below.

   d. **Army silhouettes** along the ground (y ~ H*0.65-0.75): Draw 8-12 soldier silhouettes as simple dark shapes -- each soldier is a small filled rectangle (body, ~4x10px) with a circle head (radius 2) and a thin line for rifle. Color: #0a0505. Space them across the width at varying X positions. Some soldiers slightly larger (foreground), some smaller (background depth).

   e. **Missile trucks**: 2-3 truck silhouettes at y~H*0.62. Each truck = rectangle body (30x12px) with 2 circle wheels (radius 4), and a diagonal line on top representing the missile launcher (angled ~30 degrees). Color: #0a0808.

   f. **Stacked armaments**: On the right side (x ~ W*0.75-0.85), draw a pile of crates/missiles -- 3-4 stacked rectangles (10x6px each) in dark green-gray (#1a1a10) with thin bright lines for missile tips.

   g. **Soldiers loading weapons**: 2-3 figures near the armament stack, slightly bent forward (body rectangle tilted), with arms extended toward the crates.

   h. **Ambient particles**: Use `this.time.addEvent` with repeat to spawn 5-8 floating ember particles (tiny orange dots that drift upward via tweens, alpha fading out).

   After drawing the war background, display the Supreme Turban sprite LARGER than before:
   - Change setScale from 1.8 to 2.8 (significantly more imposing)
   - Position at center: W/2, H*0.35
   - Keep the fade-in tween (alpha 0 to 1)
   - Keep the menacing pulse tween but slow it (duration 1200ms instead of 800ms, alpha range 0.8-1.0 instead of 0.7-1.0 so he stays more visible)
   - Add a red vignette glow behind the boss: large semi-transparent red circle (radius 120, rgba(255,0,0,0.15)) centered on the sprite

   Keep the "SUPREME TURBAN" text label but increase font size from 16px to 22px and add stronger glow (blur 25 instead of 15).

4. **Page 4** (fortress) -- add subtle fire glow in background (1-2 distant orange circles at low alpha).

5. **Pages 2, 5, 6** -- keep as-is (they work well narratively).

6. **All page backgrounds** should use dark, warm color palettes (reds, oranges, dark browns) instead of the current cool blues/grays, to maintain the war atmosphere throughout.

Preserve: the same 6-page text content, same targetScene ('FlightRouteScene'), same targetSceneData ({ level: 6, nextScene: 'BossScene' }), same imports, same class structure extending BaseCinematicScene.
  </action>
  <verify>
    <automated>cd C:/Users/Seba/Desktop/ORL3/superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>LastStandCinematicScene plays villain music (slow drums + brass), page 3 shows detailed war background with fire columns, smoke plumes, soldier silhouettes, missile trucks, stacked armaments, loading soldiers, and floating ember particles. Supreme Turban sprite is at scale 2.8 with red vignette glow, 22px label with strong glow. All page backgrounds use warm dark war palette.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd superzion && npx vite build` completes without errors
2. Open game in browser, navigate to Level 6 (or use menu to select LEVEL 6: OPERATION LAST STAND)
3. Verify: dark dramatic music starts immediately -- slow grave drum beats (~55 BPM), low ominous drone, descending tension arpeggio
4. Advance to page 3 (Supreme Turban reveal): war background visible with fire, smoke, soldiers, trucks, missiles
5. Supreme Turban sprite appears large (scale 2.8) with bright red glowing eyes and golden glowing crescent staff
6. Ember particles float upward in the background
7. All 6 pages advance correctly with SPACE, ESC skips to FlightRouteScene
</verification>

<success_criteria>
- playVillainMusic() produces audibly distinct dark dramatic music: slow drums (not fast trance), low brass drone, minor key tension
- War background has at minimum: fire columns, smoke, soldier silhouettes, missile trucks, armament crates (all visible as distinct shapes)
- Supreme Turban sprite is noticeably larger and more imposing than before, with visibly brighter red eye glow and golden staff glow
- Scene transitions work correctly (pages advance, ESC skips, reaches FlightRouteScene)
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/36-supreme-turban-cinematic/36-01-SUMMARY.md`
</output>
