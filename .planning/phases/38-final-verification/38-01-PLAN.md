---
phase: 38-final-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/scenes/*.js
  - superzion/src/utils/*.js
  - superzion/src/systems/*.js
  - superzion/src/entities/*.js
  - superzion/src/ui/*.js
  - superzion/src/main.js
autonomous: true
requirements: [VRFY-01]

must_haves:
  truths:
    - "Vite build completes with zero errors"
    - "Every scene.start() target matches a registered scene key in main.js"
    - "Every import statement resolves to an existing file with the referenced export"
    - "No undefined method calls on SoundManager, MusicManager, or other shared systems"
    - "No stale references to removed/renamed code from phases 31-37"
  artifacts:
    - path: "superzion/dist/index.html"
      provides: "Successful production build output"
    - path: "superzion/dist/assets/*.js"
      provides: "Bundled game code without build errors"
  key_links:
    - from: "superzion/src/main.js"
      to: "all scene files"
      via: "import + Phaser config scene array"
      pattern: "scene: \\[.*\\]"
    - from: "every scene"
      to: "next scene"
      via: "scene.start() calls"
      pattern: "scene\\.start\\("
    - from: "all scenes"
      to: "SoundManager/MusicManager"
      via: "method calls"
      pattern: "SoundManager\\.get\\(\\)\\.|MusicManager\\.get\\(\\)\\."
---

<objective>
Perform a comprehensive static audit and build verification of the entire SuperZion codebase after phases 31-37 (v1.5 Megafix v3). Identify and fix any broken references, undefined calls, stale code, or scene chain gaps.

Purpose: Ensure all changes from 7 phases of work (intro audio, platformer redesign, physics rework, level 4 redesign, B-2/explosion, villain cinematic, victory scene) integrate correctly without runtime errors.
Output: Clean build, verified scene chain, all issues fixed.
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and static import/export audit</name>
  <files>superzion/src/main.js, superzion/src/scenes/*.js, superzion/src/utils/*.js, superzion/src/systems/*.js, superzion/src/entities/*.js, superzion/src/ui/*.js</files>
  <action>
Run `cd superzion && npx vite build` and confirm zero errors.

Then perform a systematic static audit across ALL source files:

1. **Scene registration audit**: Read main.js scene array. For every scene class imported, confirm:
   - The file exists and exports a default class
   - The super() key matches what other scenes reference in scene.start()
   - Note: PortSwapScene.js exports class with super('BeirutRadarScene') -- this is intentional, not a bug

2. **Scene transition chain audit**: For EVERY scene.start() call in the codebase, verify the target scene key exists as a registered scene in main.js. The complete chain must be verified:
   - Boot -> GameIntroScene -> MenuScene
   - Menu Level 1: IntroCinematicScene -> FlightRouteScene -> PlatformerScene -> GameScene -> ExplosionCinematicScene -> (win) BeirutIntroCinematicScene
   - Menu Level 2: BeirutIntroCinematicScene -> FlightRouteScene -> BeirutRadarScene -> DeepStrikeIntroCinematicScene
   - Menu Level 3: DeepStrikeIntroCinematicScene -> FlightRouteScene -> BomberScene -> UndergroundIntroCinematicScene
   - Menu Level 4: UndergroundIntroCinematicScene -> FlightRouteScene -> DroneScene -> MountainBreakerIntroCinematicScene
   - Menu Level 5: MountainBreakerIntroCinematicScene -> FlightRouteScene -> B2BomberScene -> LastStandCinematicScene
   - Menu Level 6: LastStandCinematicScene -> FlightRouteScene -> BossScene -> VictoryScene -> CreditsScene -> MenuScene
   - Defeat paths: all showDefeatScreen calls must reference valid scene keys for both currentScene and skipScene

3. **Import/export audit**: For every `import { X } from './Y.js'`, confirm Y.js actually exports X. Pay special attention to files modified in phases 31-37:
   - PlatformerScene.js (new in Phase 32) -- imports from PlatformerLevel1Textures.js, SoundManager, MusicManager, ControlsOverlay
   - VictoryScene.js (rewritten in Phase 37) -- imports from BaseCinematicScene, SoundManager, MusicManager
   - LastStandCinematicScene.js (modified in Phase 36) -- imports from CinematicTextures, ParadeTextures
   - B2BomberScene.js (modified in Phase 35) -- imports from EndScreen, ControlsOverlay
   - DroneScene.js (modified in Phase 34) -- imports from EndScreen, ControlsOverlay
   - BomberScene.js (modified in Phase 33) -- imports from EndScreen, ControlsOverlay
   - GameIntroScene.js (modified in Phase 31) -- imports from SoundManager, MusicManager, ParadeTextures

4. **Method call audit**: Grep for all SoundManager.get().X and MusicManager.get().X calls across the codebase. Cross-reference with the actual methods defined in SoundManager.js and MusicManager.js. Flag any calls to methods that don't exist.

5. **Stale reference check**: Search for any references to old names/patterns that were replaced in phases 31-37:
   - "Signal Storm" or "Port Swap" in display strings (should be "Explosive Interception" per Phase 33)
   - Any references to deleted intro music functions
   - Any VictoryFinalScene references (replaced by VictoryScene in Phase 37)

If ANY issues are found, fix them immediately. Document all findings (clean or fixed) in the summary.
  </action>
  <verify>
    <automated>cd superzion && npx vite build 2>&1 | grep -E "error|warning|built in"</automated>
  </verify>
  <done>
    - Vite build completes with zero errors
    - Every scene.start() target verified as registered scene
    - Every import resolves to existing export
    - No undefined SoundManager/MusicManager method calls
    - No stale "Signal Storm" or "VictoryFinalScene" references remain in display strings
    - All issues found are fixed and documented
  </done>
</task>

<task type="auto">
  <name>Task 2: Scene flow and data-passing integrity audit</name>
  <files>superzion/src/scenes/FlightRouteScene.js, superzion/src/scenes/ExplosionCinematicScene.js, superzion/src/ui/EndScreen.js, superzion/src/scenes/PlatformerScene.js, superzion/src/scenes/GameScene.js</files>
  <action>
Verify that data passing between scenes is correct and no scene will crash due to missing expected data:

1. **FlightRouteScene data audit**: FlightRouteScene receives `{ level: N, nextScene: 'X' }` from every intro cinematic. Verify:
   - FlightRouteScene reads `this.nextScene` from init data (check how it accesses the passed data)
   - All 6 level values (1-6) are handled (destinations array or similar)
   - The nextScene values passed by each intro cinematic match actual scene keys

2. **ExplosionCinematicScene data audit**: Receives stats data from GameScene. Verify:
   - It gracefully handles the stats object or defaults if missing
   - Its nextScene target (BeirutIntroCinematicScene) is correct for Level 1 completion

3. **PlatformerScene -> GameScene transition**: PlatformerScene calls scene.start('GameScene'). Verify:
   - GameScene doesn't expect data that PlatformerScene doesn't pass
   - Or that GameScene handles missing init data gracefully

4. **EndScreen data audit**: EndScreen.js showVictoryScreen/showDefeatScreen receive opts with nextScene/skipScene/currentScene. Verify:
   - All callers pass required fields
   - The retry flow (currentScene) points back to the correct gameplay scene, not a cinematic

5. **BossScene -> VictoryScene transition**: Verify BossScene passes to VictoryScene correctly and VictoryScene doesn't expect specific data it won't receive.

If ANY data-passing issues are found that would cause runtime errors, fix them. Document findings.
  </action>
  <verify>
    <automated>cd superzion && npx vite build 2>&1 | grep -E "error|built in"</automated>
  </verify>
  <done>
    - FlightRouteScene correctly receives level + nextScene from all 6 intro cinematics
    - ExplosionCinematicScene handles stats data from GameScene
    - PlatformerScene -> GameScene transition verified (no missing expected data)
    - All EndScreen callers pass required opts
    - BossScene -> VictoryScene -> CreditsScene chain verified
    - Any data-passing issues fixed
  </done>
</task>

</tasks>

<verification>
1. `cd superzion && npx vite build` completes with zero errors
2. No scene.start() targets an unregistered scene key
3. No import references a non-existent export
4. No SoundManager/MusicManager method calls reference undefined methods
5. No stale display strings from pre-Phase 33 naming remain
6. All scene data-passing verified for 6 complete level flows
</verification>

<success_criteria>
- Production build succeeds
- Complete scene chain from Boot through all 6 levels to Credits verified
- All imports, exports, and method calls confirmed valid
- Zero stale references from phases 31-37 changes
- Any issues found are fixed with documented changes
</success_criteria>

<output>
After completion, create `.planning/phases/38-final-verification/38-01-SUMMARY.md`
</output>
