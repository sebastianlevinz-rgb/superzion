---
phase: 32-level-1-platformer-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/utils/BombermanTextures.js
  - superzion/src/data/LevelConfig.js
autonomous: true
requirements: [LVL1-07, LVL1-08, LVL1-09]

must_haves:
  truths:
    - "Star of David appears at center of chest on top-down Bomberman sprite, not at neck level"
    - "Key spawns visibly in Bomberman phase, can be picked up, and opens the golden door"
    - "Bomberman gameplay mechanics are fully intact after platformer scene addition"
  artifacts:
    - path: "superzion/src/utils/BombermanTextures.js"
      provides: "Star of David position fix on top-down player sprite"
      contains: "cy + 3"
    - path: "superzion/src/data/LevelConfig.js"
      provides: "Key generation verification"
      contains: "generateMap"
  key_links:
    - from: "superzion/src/utils/BombermanTextures.js"
      to: "superzion/src/scenes/GameScene.js"
      via: "generateBombermanTextures called in GameScene.create()"
      pattern: "generateBombermanTextures"
    - from: "superzion/src/data/LevelConfig.js"
      to: "superzion/src/scenes/GameScene.js"
      via: "generateMap() called in GameScene.create() for grid and powerups"
      pattern: "generateMap"
---

<objective>
Fix the Star of David position on the Bomberman sprite and verify key pickup functionality in the Bomberman phase.

Purpose: Two existing bugs/issues need fixing: the Star of David appears too high (neck level) on the top-down player sprite, and the key functionality in Bomberman needs verification. These are independent of the new platformer scene.
Output: BombermanTextures.js with corrected Star of David position, verified key spawn/pickup in LevelConfig.js.
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-level-1-platformer-redesign/32-RESEARCH.md

@superzion/src/utils/BombermanTextures.js
@superzion/src/data/LevelConfig.js
@superzion/src/scenes/GameScene.js (reference -- DO NOT modify)

<interfaces>
<!-- Key code sections to modify -->

From superzion/src/utils/BombermanTextures.js (lines 675-684, Star of David section):
```javascript
// ── Star of David ──
if (starOD) {
  // Brighter gold on chest center (lowered to center of chest area)
  starOfDavid(ctx, cx, cy + 5, 4, '#FFD700');
  // Fill inner area with semi-transparent gold
  ctx.fillStyle = 'rgba(255, 215, 0, 0.3)';
  ctx.beginPath();
  ctx.arc(cx, cy + 5, 2, 0, Math.PI * 2);
  ctx.fill();
}
```
The torso body is drawn from ~cy-2 to ~cy+10 (12px). Vest overlay from cy-1 to cy+9 (10px).
Optical center of chest = cy+3 to cy+4.
Current position cy+5 is lower-chest/belly area.
Fix: change cy+5 to cy+3 for true chest center.

From superzion/src/data/LevelConfig.js generateMap() (key generation):
```javascript
// Key spawns in the map via powerups array
// GameScene._checkPowerups() handles pickup
// Golden door (T_GDOOR) requires key
```

From superzion/src/scenes/GameScene.js:
```javascript
// GameScene remains UNCHANGED -- this plan only touches textures and config
generateBombermanTextures(this);
const { map, powerups } = generateMap();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Star of David position and verify key spawn in Bomberman</name>
  <files>superzion/src/utils/BombermanTextures.js, superzion/src/data/LevelConfig.js</files>
  <action>
**Star of David fix (BombermanTextures.js):**

1. Read `BombermanTextures.js` and find the Star of David section (around line 675-684)
2. Change BOTH occurrences of `cy + 5` to `cy + 3`:
   - Line ~678: `starOfDavid(ctx, cx, cy + 3, 4, '#FFD700');`
   - Line ~682: `ctx.arc(cx, cy + 3, 2, 0, Math.PI * 2);`
3. Update the comment to reflect the fix: "Centered on chest (vest area cy-1 to cy+9, center = cy+4, optical center = cy+3)"
4. This applies to ALL 4 directional frames (down, up, left, right) since the starOfDavid call is inside the shared body-drawing function that runs for each direction

**Rationale for cy+3:** The tactical vest spans cy-1 to cy+9 (10px tall). Mathematical center = cy+4. But the torso visual mass is denser at the upper chest, so optical center is cy+3. The original cy+5 was closer to the belly/lower chest area, reading as "too low" or "at belly" rather than "neck level" as the requirement says. However, the requirement says it's at neck level -- so the current cy+5 might actually be correct and the visual issue is elsewhere. Read the full drawing code to verify.

IMPORTANT: Before changing, carefully examine the full `_drawBombermanFrame` function to understand the sprite coordinate system. The sprite is 32x32, so `cy = 16`. Check:
- Head/helmet position (should be above cy)
- Neck position (around cy-2 to cy)
- Chest position (around cy to cy+6)
- Belly position (around cy+6 to cy+10)

If cy+5 is indeed at neck level visually (because the helmet/head takes more space than expected), adjust downward more aggressively to cy+6 or cy+7. If cy+5 is at belly level, adjust upward to cy+3. The goal is: when you look at the 32x32 sprite, the Star of David should clearly sit at the center of the chest.

**Key spawn verification (LevelConfig.js):**

1. Read `LevelConfig.js` fully, specifically the `generateMap()` function
2. Verify that the key powerup spawns in the powerups array
3. Verify the key spawns in an accessible location (not behind walls, not in zone 3 before zone 2 is opened)
4. Read `GameScene.js` `_checkPowerups()` method to verify key pickup logic:
   - Does it detect player proximity to key?
   - Does it set `this.hasKey = true`?
   - Does it update the golden door to be openable?
5. If the key spawn is randomized, verify it always spawns (not conditionally)
6. If any bugs found: fix them. Common issues:
   - Key spawns inside a wall tile (unreachable)
   - Key sprite too small or transparent (invisible)
   - Pickup radius too small
   - Golden door doesn't check `hasKey` flag
7. If everything works correctly, add a brief comment noting verification: `// Verified: key always spawns in accessible location (Phase 32, LVL1-08)`

**DO NOT modify GameScene.js** -- only touch BombermanTextures.js and LevelConfig.js.
  </action>
  <verify>
    <automated>cd superzion && grep -n "cy + 3\|cy+3" src/utils/BombermanTextures.js | head -5 && echo "---" && grep -c "key\|KEY\|T_GDOOR" src/data/LevelConfig.js</automated>
  </verify>
  <done>Star of David is centered on chest (not at neck level) on the Bomberman sprite in all 4 directions. Key generation in LevelConfig.js verified to spawn in accessible location. Key pickup and golden door mechanics confirmed working in GameScene. No GameScene modifications needed.</done>
</task>

</tasks>

<verification>
- BombermanTextures.js: Star of David Y offset changed from cy+5 to the correct chest-center value
- LevelConfig.js: Key spawn verified in generateMap() -- spawns in accessible location
- GameScene.js: NOT modified (Bomberman gameplay preserved intact per LVL1-07)
- Run `npm run dev`, start Level 1, play Bomberman phase: verify star on chest, find key, open golden door
</verification>

<success_criteria>
- Star of David visually centered on chest when viewing the top-down player sprite
- Key is visible in the Bomberman grid, can be walked over to pick up
- After picking up key, golden door opens when approached
- All existing Bomberman mechanics unchanged (bombs, guards, plant, escape, explosion)
</success_criteria>

<output>
After completion, create `.planning/phases/32-level-1-platformer-redesign/32-03-SUMMARY.md`
</output>
