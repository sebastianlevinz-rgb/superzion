---
phase: 32-level-1-platformer-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/utils/PlatformerLevel1Textures.js
  - superzion/src/main.js
  - superzion/src/scenes/IntroCinematicScene.js
autonomous: true
requirements: [LVL1-02, LVL1-03, LVL1-04]

must_haves:
  truths:
    - "Tehran night skyline textures exist with Azadi Tower, Milad Tower, Alborz mountains, moon, stars"
    - "Rooftop platform textures look like Iranian architecture (flat roofs, balconies, cornices, mosque domes)"
    - "Obstacle textures exist for security cameras, guards, searchlights, electric wire"
    - "PlatformerScene is registered in main.js scene array and IntroCinematicScene routes to it"
  artifacts:
    - path: "superzion/src/utils/PlatformerLevel1Textures.js"
      provides: "All procedural canvas textures for the platformer phase"
      exports: ["createPlatformerLevel1Textures"]
    - path: "superzion/src/main.js"
      provides: "PlatformerScene registered in Phaser scene array"
      contains: "PlatformerScene"
    - path: "superzion/src/scenes/IntroCinematicScene.js"
      provides: "Scene flow routes Level 1 through PlatformerScene"
      contains: "nextScene: 'PlatformerScene'"
  key_links:
    - from: "superzion/src/main.js"
      to: "superzion/src/scenes/PlatformerScene.js"
      via: "import and scene array registration"
      pattern: "import PlatformerScene"
    - from: "superzion/src/scenes/IntroCinematicScene.js"
      to: "FlightRouteScene"
      via: "nextScene parameter in _initPages"
      pattern: "nextScene.*PlatformerScene"
---

<objective>
Create all procedural canvas textures for the Level 1 platformer phase and wire PlatformerScene into the game's scene flow.

Purpose: The platformer textures must exist before the PlatformerScene can use them, and the scene flow must be updated so Level 1 routes through the new platformer before Bomberman.
Output: PlatformerLevel1Textures.js with all canvas-drawn textures, main.js and IntroCinematicScene.js updated for new scene flow.
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-level-1-platformer-redesign/32-RESEARCH.md

@superzion/src/main.js
@superzion/src/scenes/IntroCinematicScene.js
@superzion/src/utils/BombermanTextures.js (reference for canvas texture pattern)
@superzion/src/scenes/BomberScene.js (reference for parallax TileSprite pattern)

<interfaces>
<!-- Key patterns the executor needs from existing codebase -->

From superzion/src/utils/BombermanTextures.js (texture creation pattern):
```javascript
export function generateBombermanTextures(scene) {
  if (scene.textures.exists('bm_floor')) return; // guard against double-create
  // ... canvas drawing ...
  scene.textures.addCanvas('bm_floor', canvas);
}
```

From superzion/src/scenes/BomberScene.js (parallax TileSprite pattern):
```javascript
this.cloudTile = this.add.tileSprite(W/2, 100, W, 150, 'cloud_layer').setDepth(-7);
// In _scrollLayers(speed, dt):
this.cloudTile.tilePositionX += dx * 0.1;
```

From superzion/src/scenes/IntroCinematicScene.js line 88:
```javascript
], 'FlightRouteScene', { level: 1, nextScene: 'GameScene' });
```

From superzion/src/scenes/FlightRouteScene.js:
```javascript
const nextScene = data.nextScene || 'GameScene';
this.nextScene = nextScene;
// Later: this.scene.start(this.nextScene);
```

From superzion/src/main.js scene array:
```javascript
scene: [BootScene, GameIntroScene, MenuScene, IntroCinematicScene, FlightRouteScene, GameScene, ExplosionCinematicScene, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlatformerLevel1Textures.js with all platformer canvas textures</name>
  <files>superzion/src/utils/PlatformerLevel1Textures.js</files>
  <action>
Create `superzion/src/utils/PlatformerLevel1Textures.js` exporting a single function `createPlatformerLevel1Textures(scene)`. All textures use the `plt_` prefix and follow the project's canvas texture pattern (check `scene.textures.exists()` before creating).

Generate these textures:

**1. Background layers (for TileSprite parallax):**
- `plt_stars_sky` (960x300): Night gradient sky (#0a0a1a to #1a1a2a to #2a2030) with 80+ stars (varying brightness 0.3-1.0, size 0.3-1.5px), large moon upper-right area (30px radius, pale yellow #e8e0c0 with soft glow ring)
- `plt_mountains` (960x200): Alborz mountain range silhouette as jagged dark peaks (#1a1a2a) against slightly lighter background, snow caps on tallest peaks (white with low alpha)
- `plt_skyline` (960x250): Tehran city skyline with recognizable landmarks:
  - Milad Tower: distinctive tall shaft (4px wide, ~120px tall) with pod/observation deck (ellipse ~12px wide) and antenna tip
  - Azadi Tower: inverted-Y arch shape (~40px wide, 70px tall) with characteristic hollow center
  - Generic building silhouettes of varying heights filling the gaps
  - Warm city glow at bottom (amber/orange gradient, low alpha)
  - All drawn in dark silhouette (#1e1e2e to #2a2a3a) with occasional lit windows (tiny yellow/orange dots)
- `plt_near_buildings` (960x180): Closer building facades in darker silhouette, taller, with more visible window grids and rooftop detail (antennas, water tanks, satellite dishes)

**2. Platform textures (static sprite, not TileSprite):**
- `plt_roof_flat` (128x40): Flat concrete rooftop with subtle edge lip, weathered gray (#4a4a4a to #5a5a5a), darker border lines, optional small details (pipe stub, vent)
- `plt_roof_balcony` (100x40): Ornate balcony ledge with railing pattern (vertical bars), terracotta/cream color (#8a6a4a), decorative trim at base
- `plt_roof_cornice` (100x32): Stone cornice ledge with carved pattern detail, lighter stone color (#7a7a6a), shadow underneath
- `plt_roof_dome` (120x60): Mosque dome top -- smooth curved dome shape (#4a6a6a teal/turquoise) with crescent finial on top, flat walking surface at top 8px (invisible collision area marked by slight flat top)

**3. Player sprite (side-view running, 64x64 frames):**
- `plt_player_idle` (64x64): SuperZion side-view standing. Dark tactical outfit (deep navy #1a2040), gold Star of David on chest (#FFD700), blue-white accents, determined face, boots on ground, arms at sides
- `plt_player_run_0` through `plt_player_run_3` (64x64 each): 4-frame run cycle. Same character, legs in different stride positions (contact, passing, stride, passing). Arms swing opposite to legs. Body has slight forward lean. Each frame clearly different leg position.
- `plt_player_jump` (64x64): Jump pose -- legs tucked slightly, arms raised, body arched upward

**4. Guard sprite (side-view patrol, 64x64 frames):**
- `plt_guard_walk_0` through `plt_guard_walk_3` (64x64 each): Iranian Revolutionary Guard. Dark olive uniform (#3a4a2a), black boots, cap/beret, AK-47 held across chest. 4-frame walk cycle similar to player but different color palette and silhouette. Face has beard stubble.

**5. Obstacle textures:**
- `plt_camera` (32x32): Wall-mounted security camera -- gray body, red indicator light (blinking via alpha), lens circle
- `plt_searchlight_base` (24x48): Searchlight fixture mounted on pole -- cylindrical base, light housing on top
- `plt_electric_wire` (128x8): Horizontal electric wire segment with small insulator dots, sparks drawn as tiny yellow/white star shapes at intervals

**6. Target building:**
- `plt_target_building` (200x300): The target building at the end of the platformer. Multi-story building facade with:
  - Concrete walls (#4a4a4a) with visible floor lines
  - Windows (some lit amber, some dark)
  - One open window at mid-height with faint golden glow (entry point)
  - Military compound feel: guards visible in windows (tiny silhouettes), Iranian flag on roof, antenna array
  - Slightly more detailed than background buildings to stand out

**7. HUD/UI elements:**
- `plt_hp_heart` (16x16): Small red heart for HP display (optional -- scene may reuse existing HUD)

Follow project conventions:
- File header with ASCII art dividers
- `export function createPlatformerLevel1Textures(scene) { ... }`
- Guard clause: `if (scene.textures.exists('plt_stars_sky')) return;`
- Use `document.createElement('canvas')` + `getContext('2d')` + `scene.textures.addCanvas(key, canvas)` pattern
- Helper functions for repeated patterns (e.g., `_drawBuilding(ctx, x, y, w, h, windowColor)`)
- 2-space indentation, no semicolons at end of statements is optional (project is inconsistent, either is fine)
  </action>
  <verify>
    <automated>cd superzion && node -e "import('./src/utils/PlatformerLevel1Textures.js').then(m => { console.log('Export found:', typeof m.createPlatformerLevel1Textures); if (typeof m.createPlatformerLevel1Textures !== 'function') process.exit(1); })" 2>&1 || echo "VERIFY: Check that file exists and exports createPlatformerLevel1Textures function"</automated>
  </verify>
  <done>PlatformerLevel1Textures.js exists, exports createPlatformerLevel1Textures, generates all plt_ prefixed textures including skyline with Azadi/Milad towers, 4 platform types, 6 player frames, 4 guard frames, obstacle textures, and target building</done>
</task>

<task type="auto">
  <name>Task 2: Wire PlatformerScene into scene flow (main.js + IntroCinematicScene.js)</name>
  <files>superzion/src/main.js, superzion/src/scenes/IntroCinematicScene.js</files>
  <action>
**main.js changes:**
1. Add import at top: `import PlatformerScene from './scenes/PlatformerScene.js';`
2. Add PlatformerScene to the scene array, placing it after FlightRouteScene and before GameScene:
   `scene: [BootScene, GameIntroScene, MenuScene, IntroCinematicScene, FlightRouteScene, PlatformerScene, GameScene, ...]`

**IntroCinematicScene.js changes:**
1. Find line 88: `}, 'FlightRouteScene', { level: 1, nextScene: 'GameScene' });`
2. Change `nextScene: 'GameScene'` to `nextScene: 'PlatformerScene'`
3. This works because FlightRouteScene reads `data.nextScene` and calls `this.scene.start(this.nextScene)` -- so the flight route will now transition to PlatformerScene instead of GameScene.

NOTE: PlatformerScene.js does not exist yet (created in Plan 02). The import in main.js will cause a build error until Plan 02 creates the file. This is expected -- both plans are part of the same phase and will be complete before testing. If the executor wants to verify intermediate state, they can create a minimal stub file first:
```javascript
// Temporary stub -- replaced by Plan 02
import Phaser from 'phaser';
export default class PlatformerScene extends Phaser.Scene {
  constructor() { super('PlatformerScene'); }
  create() { this.scene.start('GameScene'); }
}
```
  </action>
  <verify>
    <automated>cd superzion && grep -c "PlatformerScene" src/main.js && grep "nextScene.*PlatformerScene" src/scenes/IntroCinematicScene.js</automated>
  </verify>
  <done>main.js imports and registers PlatformerScene in the scene array. IntroCinematicScene.js routes Level 1 through PlatformerScene (FlightRoute -> PlatformerScene -> GameScene).</done>
</task>

</tasks>

<verification>
- PlatformerLevel1Textures.js exists and exports createPlatformerLevel1Textures
- All texture keys use plt_ prefix
- main.js imports PlatformerScene and includes it in scene array
- IntroCinematicScene.js passes nextScene: 'PlatformerScene' to FlightRouteScene
- No existing files broken (GameScene untouched)
</verification>

<success_criteria>
- PlatformerLevel1Textures.js has 20+ distinct canvas textures covering skyline, platforms, player, guards, obstacles, and target building
- Scene flow updated: IntroCinematic -> FlightRoute -> PlatformerScene -> GameScene
- All textures follow project's procedural canvas pattern with existence guard
</success_criteria>

<output>
After completion, create `.planning/phases/32-level-1-platformer-redesign/32-01-SUMMARY.md`
</output>
