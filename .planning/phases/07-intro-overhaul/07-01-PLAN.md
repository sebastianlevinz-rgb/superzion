---
phase: 07-intro-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/scenes/GameIntroScene.js
autonomous: true
requirements: [INTRO-01, INTRO-02]

must_haves:
  truths:
    - "Four waving flag sprites (Iran, Lebanon, Palestine, Israel) are visible in Act 1 of the intro"
    - "Flags animate with the existing wave animation from ParadeTextures"
    - "Act 3 displays a giant golden semi-transparent Star of David behind the hero"
    - "SUPERZION title uses a wide thick arcade-style font, not monospace"
    - "Subtitle below the title is larger than the current 16px"
  artifacts:
    - path: "superzion/src/scenes/GameIntroScene.js"
      provides: "Flag placement in Act 1, title restyle in Act 3"
      contains: "flag_iran"
  key_links:
    - from: "GameIntroScene._startAct1()"
      to: "ParadeTextures flag sprites"
      via: "this.add.sprite + .play() using generated texture keys"
      pattern: "flag_iran_wave|flag_lebanon_wave|flag_palestine_wave|flag_israel_wave"
    - from: "GameIntroScene._startAct3()"
      to: "Phaser Graphics API"
      via: "fillTriangle for Star of David geometry"
      pattern: "fillTriangle.*FFD700"
---

<objective>
Restore 4 waving flag animations in Act 1 and restyle the Act 3 title screen with a giant golden Maguen David and arcade-style font.

Purpose: The intro parade should display the enemy faction flags alongside bosses (INTRO-01), and the finale should have the dramatic title presentation specified in the requirements (INTRO-02).
Output: Updated GameIntroScene.js with flag sprites in Act 1 and restyled title in Act 3.
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-intro-overhaul/07-RESEARCH.md
@.planning/phases/06-visual-identity/06-02-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs from the existing codebase -->

From superzion/src/scenes/GameIntroScene.js (line 13-14):
```javascript
import { generateAllParadeTextures } from '../utils/ParadeTextures.js';
// generateAllParadeTextures(this) is called in create() — generates all flag + parade textures
```

Boss placement positions (Act 1, lines 84-114):
```javascript
// Boss 1: x=150, Boss 2: x=W/2-100, Boss 3: x=W/2+100, Boss 4: x=W-150
// All at y = H/2 + 40, with labels at y+50
// Boss timing: 1200ms, 2800ms, 4400ms, 6000ms
```

Flag texture keys generated by ParadeTextures.js:
```javascript
// Sprite keys: 'flag_iran', 'flag_lebanon', 'flag_palestine', 'flag_israel'
// Animation keys: 'flag_iran_wave', 'flag_lebanon_wave', 'flag_palestine_wave', 'flag_israel_wave'
// Each flag is 140x104 pixels (140x90 base + 14px padding), 4 frames
```

Act 3 title section (lines 392-441):
```javascript
// At 5000ms: white flash + SUPERZION title (currently monospace 64px, gold)
// At 6500ms: subtitle "ONE SOLDIER. SIX MISSIONS. ZERO MERCY." (currently monospace 16px, white)
// Hero sprite: 'parade_superzion' at depth 20, walks to W/2+60
```

BaseCinematicScene provides: W=960, H=540
actObjects pattern: push all created objects for cleanup via _clearAct()
Guard pattern: `if (this.skipped) return;` at top of every delayed callback
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 waving flag sprites to Act 1</name>
  <files>superzion/src/scenes/GameIntroScene.js</files>
  <action>
In `_startAct1()`, after the fire glow section and before the "AXIS OF EVIL" title text (around line 69), add flag sprite instantiation:

1. Create an array of flag data mapping each boss to their enemy flag:
   - Iran flag (flag_iran) at x=150, behind Boss 1 (Foam Beard)
   - Lebanon flag (flag_lebanon) at x=W/2-100, behind Boss 2 (Turbo Turban / Hezbollah)
   - Palestine flag (flag_palestine) at x=W/2+100, behind Boss 3 (The Warden / Hamas)
   - Israel flag (flag_israel) at x=W-150, as the 4th flag (narrative: Israel is the target)

2. For each flag:
   - Create sprite: `this.add.sprite(x, H/2 - 10, key)` — position slightly above boss y (H/2+40) so flags appear behind/above bosses
   - Set depth 4 (below bosses at depth 15, below boss aura at 14)
   - Set scale 0.6 (flags at 140x104 native = ~84x62 displayed, appropriate for parade context)
   - Set initial alpha 0
   - Play wave animation: `flag.play(key + '_wave')`
   - Push to `this.actObjects`
   - Fade in with stagger: tween alpha from 0 to 1, duration 500ms, delay staggered (0ms, 200ms, 400ms, 600ms) so flags appear left-to-right before bosses enter at 1200ms

3. The Israel flag at x=W-150 will visually pair with the Supreme Turban boss (appearing last), creating a narrative of "all nations involved."

Do NOT modify ParadeTextures.js — flag textures are already generated.
Do NOT change boss positions or timing — flags are background decoration behind bosses.
  </action>
  <verify>
    <automated>cd superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Four flag sprites with wave animations appear in Act 1 at the four boss positions, fading in before bosses enter. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Act 3 title with Maguen David and arcade font</name>
  <files>superzion/src/scenes/GameIntroScene.js</files>
  <action>
In `_startAct3()`, modify the title sequence starting at the 5000ms delayedCall (line 393):

1. **Giant golden semi-transparent Maguen David** — Add BEFORE the title text, inside the same 5000ms callback:
   - Create a Phaser graphics object at depth 19 (below title at 50, above hero at 20)
   - Draw two overlapping equilateral triangles forming a Star of David:
     - Center: cx = W/2, cy = H/2 - 20 (slightly above center, aligned with title area)
     - Radius: r = 110 (large, prominent)
     - Fill color: 0xFFD700 (gold), alpha 0.25 (semi-transparent)
     - Upward triangle: fillTriangle(cx, cy-r, cx - r*0.866, cy + r*0.5, cx + r*0.866, cy + r*0.5)
     - Downward triangle: fillTriangle(cx, cy+r, cx - r*0.866, cy - r*0.5, cx + r*0.866, cy - r*0.5)
   - Start with alpha 0, tween to alpha 1 over 600ms (fades in behind everything)
   - Push graphics object to actObjects

2. **SUPERZION arcade font** — Replace the existing title text config:
   - Change fontFamily from `'monospace'` to `'"Impact", "Arial Black", "Trebuchet MS", sans-serif'`
   - Change fontSize from `'64px'` to `'72px'` (larger)
   - Add letter spacing by changing text from `'SUPERZION'` to `'S U P E R Z I O N'` (spaced letters for wide arcade look)
   - Keep the gold color (#FFD700) and shadow blur (40)
   - Keep the impact zoom animation (scale 3 -> 1 with Back.easeOut) and breathing pulse

3. **Larger subtitle** — Modify the 6500ms delayedCall subtitle:
   - Change fontSize from `'16px'` to `'24px'`
   - Change fontFamily from `'monospace'` to `'"Impact", "Arial Black", "Trebuchet MS", sans-serif'`
   - Keep white color and shadow blur

4. Remove the red underline bar (lines 427-429) — it conflicts visually with the Star of David behind the text. The Maguen David provides sufficient visual framing.

Do NOT change the hero walk-in animation or timing.
Do NOT change the white flash or camera shake at 5000ms — those remain.
Do NOT change the 7000ms secondary shake or 8000ms gold flash.
  </action>
  <verify>
    <automated>cd superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Act 3 title screen shows a giant golden semi-transparent Star of David behind the hero, "S U P E R Z I O N" in wide thick Impact/Arial Black font at 72px, and subtitle at 24px in the same font family. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd superzion && npx vite build` completes with no errors
2. Visual check: Launch dev server (`npm run dev`), watch Act 1 for 4 waving flags behind boss positions
3. Visual check: Watch Act 3 for golden Star of David, arcade-style SUPERZION title, larger subtitle
4. Skip test: Press ENTER during intro to verify skip still works cleanly (no destroyed object errors)
</verification>

<success_criteria>
- Four waving flag sprites visible in Act 1, each at the corresponding boss x-position, with wave animation playing
- Act 3 shows a golden semi-transparent Star of David (two overlapping triangles) behind the hero sprite
- "S U P E R Z I O N" renders in Impact/Arial Black at 72px with gold glow
- Subtitle renders at 24px in the same font family
- Intro skip (ENTER) works without console errors
- Vite build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-intro-overhaul/07-01-SUMMARY.md`
</output>
