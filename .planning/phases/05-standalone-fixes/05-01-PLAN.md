---
phase: 05-standalone-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/scenes/PortSwapScene.js
autonomous: false
requirements:
  - GAME-01

must_haves:
  truths:
    - "Player can navigate from entrance (80, 490) to any container yard (north, south, east) without getting stuck"
    - "Player can reach any target container the intel system selects and interact with it"
    - "Player can return from the target container back to the entrance/exit to complete the mission"
  artifacts:
    - path: "superzion/src/scenes/PortSwapScene.js"
      provides: "Level 2 container layout with passable corridors"
      contains: "CONT_ROW_SPACING"
  key_links:
    - from: "superzion/src/scenes/PortSwapScene.js"
      to: "Phaser Arcade Physics"
      via: "staticGroup colliders on containers and walls"
      pattern: "body\\.setSize"
---

<objective>
Fix Level 2 (Operation Port Swap) so the player can navigate container corridors from the entrance to any target container and back to the exit without getting stuck on walls or obstacles.

Purpose: Level 2 is currently unbeatable because the player gets stuck navigating container yards. This is a shipping blocker.
Output: Modified PortSwapScene.js with passable container layout verified by play-testing.
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-standalone-fixes/05-RESEARCH.md

@superzion/src/scenes/PortSwapScene.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Level 2 container passability</name>
  <files>superzion/src/scenes/PortSwapScene.js</files>
  <action>
Investigate and fix the Level 2 container yard passability issue. The level uses PortSwapScene.js (registered as scene key 'BeirutRadarScene'). Do NOT modify the old BeirutRadarScene.js file.

**Current state:**
- Container body: 40w x 18h (visual: 48w x 24h)
- Player body: 14w x 14h
- CONT_ROW_SPACING = 48 (vertical between centers, leaves 30px gap between body edges = 16px clearance)
- CONT_COL_SPACING = 72 (horizontal between centers, leaves 32px gap between body edges = 18px clearance)
- Player spawns at (80, 490), entrance/exit marker at same position
- Three container yards: north (base 160,130), south (base 160,340), east (base 560,160) plus 5 scattered east extras
- Top water wall: invisible rectangle at (480, HUD_H+30=80) with body 960x60, so wall bottom edge is at y=110
- North yard first container row at y=130, gap from wall to first row = 130 - 110 = 20px. Player body is 14h + offset(13) = body bottom at y+27 from sprite top. This 20px gap is tight.

**Investigation steps (run the dev server, read code carefully):**
1. Calculate exact pixel clearance between the top water wall bottom edge (y=110) and north yard first row containers (y=130, body top at y-9=121). Gap = 121-110 = 11px. Player body is 14px tall. This is the likely chokepoint — player CANNOT fit between top wall and north container row.
2. Check corridor between north and south yards (y=226 bottom of north yard to y=331 top of south yard). Gap should be ~105px — plenty of room.
3. Check path from entrance (80, 490) to yards. Left wall at x=32, player at x=80, should be fine.
4. Check if east yard containers at (560,160) to (776,208) conflict with east extras at (560,340)-(632,388). Gap should be fine.

**Fix approach:**
The primary fix is to increase clearance between the top water wall and the north container yard. Options:
- Move north yard containers down (increase base y from 130 to 140-145)
- Make the top wall thinner (reduce height from 60 to 40-50)
- Or both — small adjustments to each

Also verify:
- Guard patrol paths (GUARD_PATROLS array) still make sense after layout changes — guards should patrol in corridors between/around container grids, not through them
- Worker positions (WORKER_SPAWNS) are still in valid corridor locations
- Security camera positions (SEC_CAMERAS) still have line-of-sight into container areas
- No container extends beyond viewport bounds (960x540 with HUD_H=50 offset)

After fixes, verify the math:
- Row clearance: CONT_ROW_SPACING - container_body_height(18) = gap. Gap must be > player_body_height(14) + 4px minimum clearance = 18px minimum gap, so spacing >= 36.
- Col clearance: CONT_COL_SPACING - container_body_width(40) = gap. Gap must be > player_body_width(14) + 4px = 18px minimum gap, so spacing >= 58.
- Top wall to first row clearance: first_row_y - container_half_body_height(9) - wall_bottom_edge > player_body_height(14) + 4px clearance.

Do NOT change the scene key ('BeirutRadarScene'). Do NOT modify any other scene files. Keep all existing game mechanics (guards, cameras, workers, suspicion, intel) intact.
  </action>
  <verify>
    <automated>cd superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Level 2 container layout has verified clearance math: all corridors between container rows/columns provide at least 18px gap (player body 14px + 4px clearance), and the path from water wall to north yard provides at least 18px gap. Build succeeds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Level 2 is playable</name>
  <action>
Human play-tests Level 2 to verify the player can navigate all container yards and complete the mission.

What was built: Level 2 container passability fix — adjusted container layout and/or wall positions so the player can navigate all container yards.

How to verify:
1. Run `cd superzion && npx vite dev` to start the dev server
2. Open http://localhost:3000 in browser
3. Select Level 2 (Operation Port Swap) from the menu
4. Navigate the player from the entrance (bottom-left) to the NORTH container yard (top area) — verify the player can enter between container rows
5. Navigate to the SOUTH container yard (middle area) — verify the player can move between rows
6. Navigate to the EAST container yard (right side) — verify the player can reach scattered containers
7. Find the target container (highlighted by intel clues) and interact with it (press E)
8. Navigate back to the entrance to complete the mission
9. The player should NOT get stuck on any wall, container, or boundary at any point

Resume signal: Type "approved" if the player can navigate all yards and complete the mission, or describe where the player gets stuck.
  </action>
  <verify>Human visual and gameplay verification</verify>
  <done>User confirms Level 2 is navigable from entrance to target and back to exit without getting stuck</done>
</task>

</tasks>

<verification>
- Vite build completes without errors
- Container spacing math verified in code comments
- Player can physically navigate all three yards and return to exit (confirmed by human checkpoint)
</verification>

<success_criteria>
- Player can navigate Level 2 container corridors from start to bomb-plant target and back to exit without getting stuck on any wall or obstacle
- No regressions in guard patrol behavior, camera detection, or worker placement
</success_criteria>

<output>
After completion, create `.planning/phases/05-standalone-fixes/05-01-SUMMARY.md`
</output>
