---
phase: 35-level-5-b-2-explosion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - superzion/src/utils/B2Textures.js
autonomous: true
requirements:
  - LVL5-01
  - LVL5-02
  - LVL5-03

must_haves:
  truths:
    - "B-2 sprite is a wide flat flying wing (boomerang/bat shape) with dark gray coloring, panel lines, and moonlight highlights -- NOT a triangle with a line"
    - "Mountain has visible texture, vegetation detail (trees/shrubs), and rock detail -- clearly looks like a mountain, not an abstract cross-section"
  artifacts:
    - path: "superzion/src/utils/B2Textures.js"
      provides: "Redesigned B-2 sprite and detailed mountain texture"
      exports: ["createB2SideSprite", "createNatanzMountain"]
  key_links:
    - from: "superzion/src/utils/B2Textures.js"
      to: "superzion/src/scenes/B2BomberScene.js"
      via: "import { createB2SideSprite, createNatanzMountain } from B2Textures.js"
      pattern: "createB2SideSprite|createNatanzMountain"
---

<objective>
Redesign the B-2 bomber sprite to look like a convincing flying wing and redraw the Natanz mountain to clearly look like a real mountain with texture, vegetation, and rock detail.

Purpose: The current B-2 sprite already has a boomerang shape but needs to be verified and potentially refined to be wider and flatter with more prominent panel lines and moonlight highlights. The mountain texture currently looks like an abstract cross-section -- it needs visible vegetation (trees, shrubs), varied rock textures, snow on peaks, and natural mountain coloring to clearly read as a mountain.
Output: Updated B2Textures.js with refined B-2 sprite and completely redrawn mountain texture.
</objective>

<execution_context>
@C:/Users/Seba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@superzion/src/utils/B2Textures.js
@superzion/src/scenes/B2BomberScene.js
</context>

<interfaces>
<!-- Key imports and constants the executor needs from B2BomberScene.js -->

From superzion/src/scenes/B2BomberScene.js:
```javascript
import {
  createB2SideSprite, createNightSky, createNightCloudLayer,
  createDesertNight, createCityNight, createMountainNight,
  createNatanzMountain,
} from '../utils/B2Textures.js';

// B-2 sprite used as:
this.jetSprite = this.add.image(this.jetX, this.jetY, 'b2_side').setDepth(10);

// Mountain used as:
this.mountainSprite = this.add.image(MTN_X, GROUND_Y - 80, 'natanz_mountain').setDepth(2);

// Mountain constants:
const MTN_X = 560;          // mountain center X in bombing phase
const MTN_TOP_Y = 110;      // mountain peak Y
const GROUND_Y = 420;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refine B-2 flying wing sprite</name>
  <files>superzion/src/utils/B2Textures.js</files>
  <action>
Rewrite the `createB2SideSprite()` function in B2Textures.js. The current sprite (140x90 canvas) already has a boomerang/flying-wing shape with swept-back leading edges and W-shaped trailing edge. Refine it to be even more convincing:

1. **Shape**: Keep the 140x90 canvas. Make the wing wider and flatter -- the wingspan should extend closer to the full canvas height (wingtips at y~4 and y~86). The body center should be thinner (less bulky nose-to-tail depth). The trailing edge W-shape should be more subtle/shallow (real B-2 has gentle serrations, not deep V-notches).

2. **Coloring**: Use dark charcoal gray (#2a2a30 base, NOT black). Add subtle color variation across the wing surface -- slightly lighter center (#33333a), slightly darker wing edges (#222228). This should read as a flat surface catching minimal light.

3. **Panel lines**: Draw 4-5 thin panel lines (0.5px, rgba(60,60,70,0.35)) running from nose to trailing edge at different angles. Add 2-3 spanwise panel lines perpendicular to the leading edge. These should be subtle -- visible but not dominating.

4. **Moonlight highlights**: Draw a bright edge highlight (rgba(180,200,230,0.6), 0.8px stroke) along the ENTIRE upper leading edge from nose to left wingtip, and a slightly dimmer one (rgba(180,200,230,0.35)) on the lower leading edge. This is the moonlight from above catching the swept-back edge.

5. **Cockpit**: Small dark diamond/trapezoid near the nose (#1a1a22) with a tiny glass reflection highlight (rgba(100,140,180,0.18)).

6. **Engine intakes/exhausts**: Two subtle elliptical bumps behind the cockpit and faint blue-white engine glow at the trailing edge near center.

7. **Outline**: 1px stroke around the full contour in rgba(160,180,210,0.55) -- already exists, keep it.

8. **Shadow**: Keep the subtle ground shadow (offset 3px down-left, rgba(0,0,0,0.12)).

The texture key MUST remain 'b2_side' so the scene import works unchanged. Keep the `scene.textures.remove('b2_side')` guard at the top.
  </action>
  <verify>
    <automated>cd superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>B-2 sprite is a wide, flat flying wing with dark gray coloring, visible panel lines, moonlight edge highlights, and subtle outline. Canvas renders without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Redraw Natanz mountain with realistic detail</name>
  <files>superzion/src/utils/B2Textures.js</files>
  <action>
Rewrite the `createNatanzMountain()` function in B2Textures.js. The current mountain (480x350 canvas) shows a cross-section view with internal layers -- but from the gameplay perspective during the bombing phase, the player sees it as a surface mountain. It needs to clearly look like a MOUNTAIN, not a diagram.

1. **Mountain silhouette**: Keep the 480x350 canvas. Draw a rugged mountain profile with an asymmetric peak (not perfectly triangular). Use multiple overlapping triangular shapes for a natural ridge-line. Left slope steeper, right slope gentler. The peak should be near y=40 (high up in the canvas).

2. **Rock texture base**: Fill the mountain body with layered brown-gray gradients (#3a3428 to #2a2820 to #1a1a18). Add random rocky patches using small irregular rectangles in varying shades of gray-brown (rgba with 0.3-0.5 alpha) across the entire surface. Add horizontal stratification lines (very thin, rgba(80,70,50,0.2)) to suggest geological layers visible on the cliff face.

3. **Vegetation**: Draw small triangular trees (dark green #1a3012) on the lower slopes (bottom 40% of the mountain). Denser at the base, sparser higher up. Use 15-25 trees of varying sizes (3-8px base). Add scrub/bush patches as small dark-green ellipses between trees.

4. **Snow/ice on peak**: Fill the top ~15% of the mountain with a lighter color (rgba(220,230,240,0.25-0.35)) to suggest snow. Add a bright white edge along the peak ridge.

5. **Rock detail higher up**: Above the tree line but below the snow, add exposed rock faces -- lighter gray patches (#4a4840) with sharp edges. Add small shadow crevices (thin dark lines, #0a0a08).

6. **Internal layers (keep but dimmer)**: The bombing phase gameplay depends on the internal layer system (`LAYER_START_Y`, `LAYER_H`, etc. in B2BomberScene.js). The mountain layers are actually drawn as separate rectangles by the SCENE (in `_startBombing()`), NOT by this texture function. So the texture just needs to look like a mountain surface. However, leave the cross-section layer visualization in the CENTER of the mountain (same layerX, layerW coordinates: cx-60, 120 wide) but make it dimmer (lower alpha) so it suggests the bunker entrance/cutaway without dominating the mountain appearance. The surface mountain detail should wrap AROUND the cross-section area.

7. **Ambient detail**: Add a faint light source from the upper-right (moonlight) -- slightly brighter right-facing slopes and darker left-facing slopes via gradient overlays.

The texture key MUST remain 'natanz_mountain'. Keep any existing texture guard at top.
  </action>
  <verify>
    <automated>cd superzion && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Mountain texture clearly reads as a mountain with visible rock texture, vegetation on lower slopes, snow on peak, and natural coloring. Internal layer system preserved but dimmer. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd superzion && npx vite build` completes without errors
- B2Textures.js exports createB2SideSprite and createNatanzMountain with unchanged function signatures
- Texture keys remain 'b2_side' and 'natanz_mountain'
</verification>

<success_criteria>
- B-2 sprite is a wide flat flying wing (boomerang shape), NOT a triangle with a line
- B-2 has dark gray coloring, panel lines, moonlight highlights on upper edges, subtle outline
- Mountain clearly looks like a mountain with texture, vegetation, and rock detail
- Build succeeds, no import/export changes needed in B2BomberScene.js
</success_criteria>

<output>
After completion, create `.planning/phases/35-level-5-b-2-explosion/35-01-SUMMARY.md`
</output>
